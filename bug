myplear 只接收一次指令(已解决):发送命令时检测命令长度使用了sizeof(),而不是strlen();
试图将dup2设置为友元函数(已解决):dup2()的函数原型为int dup2(int ,int )throw();
程序阻塞在打开管道(已解决):使用只读,只写打开管道,没有相反操作时会造成open阻塞;
使用 q 键关闭程序时,mplare会在后台有残留进程(已解决):在退出函数中添加 kill()函数杀死mplare进程;

更换歌曲时手机进度不清零(已解决):在下一首的调用函数中添加进度条清零语句;
更换歌曲后进度条不走():;
